/**
 * Crash Reporter Service
 * 
 * Export crash reporter service and types
 */

export { CrashReporter, crashReporter, CrashSeverity } from './crashReporter';
export type { CrashContext, DeviceInfo, Breadcrumb } from './crashReporter';
